name: intercom_flutter
description: Flutter plugin for Intercom integration. Provides in-app messaging
  and help-center Intercom services
version: 1.0.0
homepage: https://github.com/v3rm0n/intercom_flutter

dependencies:
  flutter:
    sdk: flutter
  # intercom_flutter_platform_interface: 0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  collection: ^1.14.13

flutter:
  plugin:
    platforms:
      android:
        package: io.maido.intercom
        pluginClass: IntercomFlutterPlugin
      ios:
        pluginClass: IntercomFlutterPlugin

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.22.4 <2.0.0"
